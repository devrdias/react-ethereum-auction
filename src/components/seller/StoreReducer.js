
//seller, buyer, owner, admin

const initialState = {
    data:{
    },
    product:[
    ]
}
  
const storeReducer = (state = initialState, action) => {
    
    
    return state
}
  
export default storeReducer
  